#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    
    // Asi encontramos min y max
    cout << max(2, 4);
    cout << min(2, 4);

    // Para este tipo de operaciones importamos la libreria cmath
    cout << round(2.8);
    cout << sqrt(2);
    cout << log(2);


    return 0;
}
